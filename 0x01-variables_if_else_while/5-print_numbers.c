#include <stdio.h>

/**
 * main - main block
 * Description: This program will print the alphabet except q & e.
 *
 * Return: 0
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	return (0);
}
